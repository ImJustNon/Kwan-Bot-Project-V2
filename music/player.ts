import { Client } from 'discord.js';
import { ClientParams } from '../types/ClientTypes';
import config from '../config/config';


async function StartNodes(client: ClientParams): Promise<void> {

}


export {

}
export default StartNodes;